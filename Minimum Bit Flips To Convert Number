class Solution {
    public int minBitFlips(int start, int goal) {
        int num = start ^ goal;
        int flips = 0;

        while(num > 0){
            num = num & (num - 1);
            flips++;
        }
        return flips;
    }
}
